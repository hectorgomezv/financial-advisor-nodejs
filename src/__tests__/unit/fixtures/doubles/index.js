const { buildCompany } = require('./company-double');
const { buildCompanyState } = require('./company-state-double');
const { buildPosition } = require('./position-double');

module.exports = {
  buildCompany,
  buildCompanyState,
  buildPosition,
};
